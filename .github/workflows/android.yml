name: Community Gadget Android

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - name: Build and test both Android apps
        run: |
          chmod +x gradlew
          ./gradlew testDebugUnitTest :app:assembleDebug :controlcenter:assembleDebug --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: community-gadget-field-and-control-apks
          path: |
            app/build/outputs/apk/debug/app-debug.apk
            controlcenter/build/outputs/apk/debug/controlcenter-debug.apk
          if-no-files-found: error
